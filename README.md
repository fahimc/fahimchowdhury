# fahimchowdhury.com

Static portfolio and writing site for Fahim Chowdhury, focused on practical AI solutions, product thinking, and useful intelligent experiences.

## Run locally

Open `index.html` directly, or serve the folder with any static server:

```bash
npx serve .
```

The `ui/` directory keeps the UI intent in `.aui` source form alongside the compiled static implementation.
